1. npm init -------------initialize
2. npm install expresss
3. npm install jsonwebtoken -------------for user authntication
4. npm install monsoose
5. npm install multer ------------store the image use in frontend
6. npm install cors ---------------application gate permission to the backend
